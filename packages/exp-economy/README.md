# @bc/exp-economy

A minimal economy expansion shell for Balance // Control. It demonstrates:
- a namespaced state slice at `state.extensions.economy`
- one safe action `exp.economy.grantCredit` with strict payload validation
- no gameplay impact; scaffolding only